#include "code.h"
#include "common.h"
#include "rawfs.h"

extern void readBlock(off_t blockNum, char *buf, int blockSize);

off_t readSuper(int *blockSize) {
/* Initialize variables return the size of a valid Minix
 * file system blocks, but zero on error.
 */
	readblock
}
